// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v5.28.3
// source: ink/model/vcard.proto

/* eslint-disable */

export const protobufPackage = "ink.model";

export enum VCardVersion {
  VCARD_VERSION_UNSPECIFIED = 0,
  /** V3_0 - RFC 2426 */
  V3_0 = 1,
  /** V4_0 - RFC 6350 */
  V4_0 = 2,
  UNRECOGNIZED = -1,
}

export function vCardVersionFromJSON(object: any): VCardVersion {
  switch (object) {
    case 0:
    case "VCARD_VERSION_UNSPECIFIED":
      return VCardVersion.VCARD_VERSION_UNSPECIFIED;
    case 1:
    case "V3_0":
      return VCardVersion.V3_0;
    case 2:
    case "V4_0":
      return VCardVersion.V4_0;
    case -1:
    case "UNRECOGNIZED":
    default:
      return VCardVersion.UNRECOGNIZED;
  }
}

export function vCardVersionToJSON(object: VCardVersion): string {
  switch (object) {
    case VCardVersion.VCARD_VERSION_UNSPECIFIED:
      return "VCARD_VERSION_UNSPECIFIED";
    case VCardVersion.V3_0:
      return "V3_0";
    case VCardVersion.V4_0:
      return "V4_0";
    case VCardVersion.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum TelType {
  TEL_TYPE_UNSPECIFIED = 0,
  VOICE = 1,
  TEXT = 2,
  FAX = 3,
  CELL = 4,
  VIDEO = 5,
  PAGER = 6,
  UNRECOGNIZED = -1,
}

export function telTypeFromJSON(object: any): TelType {
  switch (object) {
    case 0:
    case "TEL_TYPE_UNSPECIFIED":
      return TelType.TEL_TYPE_UNSPECIFIED;
    case 1:
    case "VOICE":
      return TelType.VOICE;
    case 2:
    case "TEXT":
      return TelType.TEXT;
    case 3:
    case "FAX":
      return TelType.FAX;
    case 4:
    case "CELL":
      return TelType.CELL;
    case 5:
    case "VIDEO":
      return TelType.VIDEO;
    case 6:
    case "PAGER":
      return TelType.PAGER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TelType.UNRECOGNIZED;
  }
}

export function telTypeToJSON(object: TelType): string {
  switch (object) {
    case TelType.TEL_TYPE_UNSPECIFIED:
      return "TEL_TYPE_UNSPECIFIED";
    case TelType.VOICE:
      return "VOICE";
    case TelType.TEXT:
      return "TEXT";
    case TelType.FAX:
      return "FAX";
    case TelType.CELL:
      return "CELL";
    case TelType.VIDEO:
      return "VIDEO";
    case TelType.PAGER:
      return "PAGER";
    case TelType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AdrType {
  ADR_TYPE_UNSPECIFIED = 0,
  HOME = 1,
  WORK = 2,
  UNRECOGNIZED = -1,
}

export function adrTypeFromJSON(object: any): AdrType {
  switch (object) {
    case 0:
    case "ADR_TYPE_UNSPECIFIED":
      return AdrType.ADR_TYPE_UNSPECIFIED;
    case 1:
    case "HOME":
      return AdrType.HOME;
    case 2:
    case "WORK":
      return AdrType.WORK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AdrType.UNRECOGNIZED;
  }
}

export function adrTypeToJSON(object: AdrType): string {
  switch (object) {
    case AdrType.ADR_TYPE_UNSPECIFIED:
      return "ADR_TYPE_UNSPECIFIED";
    case AdrType.HOME:
      return "HOME";
    case AdrType.WORK:
      return "WORK";
    case AdrType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum EmailType {
  EMAIL_TYPE_UNSPECIFIED = 0,
  EMAIL_HOME = 1,
  EMAIL_WORK = 2,
  UNRECOGNIZED = -1,
}

export function emailTypeFromJSON(object: any): EmailType {
  switch (object) {
    case 0:
    case "EMAIL_TYPE_UNSPECIFIED":
      return EmailType.EMAIL_TYPE_UNSPECIFIED;
    case 1:
    case "EMAIL_HOME":
      return EmailType.EMAIL_HOME;
    case 2:
    case "EMAIL_WORK":
      return EmailType.EMAIL_WORK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EmailType.UNRECOGNIZED;
  }
}

export function emailTypeToJSON(object: EmailType): string {
  switch (object) {
    case EmailType.EMAIL_TYPE_UNSPECIFIED:
      return "EMAIL_TYPE_UNSPECIFIED";
    case EmailType.EMAIL_HOME:
      return "EMAIL_HOME";
    case EmailType.EMAIL_WORK:
      return "EMAIL_WORK";
    case EmailType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface VCardName {
  /** Family name (surname) */
  family: string;
  /** Given name (first name) */
  given: string;
  /** Additional names (middle) */
  additional: string;
  /** Honorific prefix (Mr., Dr.) */
  prefix: string;
  /** Honorific suffix (Jr., III) */
  suffix: string;
}

export interface VCardAddress {
  type: AdrType;
  poBox: string;
  /** Extended address (apt, suite) */
  extended: string;
  street: string;
  /** City */
  locality: string;
  /** State/province */
  region: string;
  postalCode: string;
  country: string;
  /** Formatted address label */
  label: string;
}

export interface VCardTelephone {
  type: TelType;
  /** Phone number (tel: URI or plain) */
  value: string;
  isPreferred: boolean;
}

export interface VCardEmail {
  type: EmailType;
  value: string;
  isPreferred: boolean;
}

export interface VCardOrganization {
  /** Organization name */
  name: string;
  /** Organizational unit (department) */
  unit: string;
}

export interface VCardImage {
  /** URL to image */
  uri: string;
  /** MIME type (image/png, image/jpeg) */
  mediaType: string;
  /** Inline binary data (base64 in jCard) */
  data: Uint8Array;
}

export interface VCardExtProperty {
  /** e.g. "X-AUTH", "X-MCP-URI" */
  name: string;
  value: string;
}

export interface InkVCard {
  /** Identification (§6.2) */
  uid: string;
  version: VCardVersion;
  /** FN — display name */
  formattedName: string;
  /** N — structured name components */
  name:
    | VCardName
    | undefined;
  /** Communication (§6.4) */
  emails: VCardEmail[];
  telephones: VCardTelephone[];
  /** Address (§6.3) */
  addresses: VCardAddress[];
  /** Organization (§6.6) */
  org:
    | VCardOrganization
    | undefined;
  /** Job title */
  title: string;
  /** Functional role */
  role: string;
  /** Media (§6.2) */
  photo: VCardImage | undefined;
  logo:
    | VCardImage
    | undefined;
  /** Web (§6.7) */
  urls: string[];
  /** Categories & Notes (§6.7) */
  categories: string[];
  note: string;
  /** Security (§6.8) */
  key: string;
  /** Timestamps */
  rev: string;
  /** ── Ink-specific extensions ──────────────────────────────────── */
  isLlm: boolean;
  /** ink roles: "edit", "view" */
  roles: string[];
  /** domain/user/ filesystem path */
  folderPath: string;
  /** mcp://model:jwt@host:port/tool */
  mcpUri: string;
  /** Full jCard JSON (RFC 7095) for JWT embedding */
  jcardJson: string;
  /** Custom X-* properties */
  extProperties: VCardExtProperty[];
  /** Language & Locale */
  language: string;
  /** TZ property (IANA timezone) */
  timezone: string;
}

export interface JCard {
  /** Raw jCard JSON string */
  json: string;
  /** Parsed vCard fields */
  parsed: InkVCard | undefined;
}

function createBaseVCardName(): VCardName {
  return { family: "", given: "", additional: "", prefix: "", suffix: "" };
}

export const VCardName: MessageFns<VCardName> = {
  fromJSON(object: any): VCardName {
    return {
      family: isSet(object.family) ? globalThis.String(object.family) : "",
      given: isSet(object.given) ? globalThis.String(object.given) : "",
      additional: isSet(object.additional) ? globalThis.String(object.additional) : "",
      prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : "",
      suffix: isSet(object.suffix) ? globalThis.String(object.suffix) : "",
    };
  },

  toJSON(message: VCardName): unknown {
    const obj: any = {};
    if (message.family !== "") {
      obj.family = message.family;
    }
    if (message.given !== "") {
      obj.given = message.given;
    }
    if (message.additional !== "") {
      obj.additional = message.additional;
    }
    if (message.prefix !== "") {
      obj.prefix = message.prefix;
    }
    if (message.suffix !== "") {
      obj.suffix = message.suffix;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VCardName>, I>>(base?: I): VCardName {
    return VCardName.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VCardName>, I>>(object: I): VCardName {
    const message = createBaseVCardName();
    message.family = object.family ?? "";
    message.given = object.given ?? "";
    message.additional = object.additional ?? "";
    message.prefix = object.prefix ?? "";
    message.suffix = object.suffix ?? "";
    return message;
  },
};

function createBaseVCardAddress(): VCardAddress {
  return {
    type: 0,
    poBox: "",
    extended: "",
    street: "",
    locality: "",
    region: "",
    postalCode: "",
    country: "",
    label: "",
  };
}

export const VCardAddress: MessageFns<VCardAddress> = {
  fromJSON(object: any): VCardAddress {
    return {
      type: isSet(object.type) ? adrTypeFromJSON(object.type) : 0,
      poBox: isSet(object.poBox)
        ? globalThis.String(object.poBox)
        : isSet(object.po_box)
        ? globalThis.String(object.po_box)
        : "",
      extended: isSet(object.extended) ? globalThis.String(object.extended) : "",
      street: isSet(object.street) ? globalThis.String(object.street) : "",
      locality: isSet(object.locality) ? globalThis.String(object.locality) : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      postalCode: isSet(object.postalCode)
        ? globalThis.String(object.postalCode)
        : isSet(object.postal_code)
        ? globalThis.String(object.postal_code)
        : "",
      country: isSet(object.country) ? globalThis.String(object.country) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
    };
  },

  toJSON(message: VCardAddress): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = adrTypeToJSON(message.type);
    }
    if (message.poBox !== "") {
      obj.poBox = message.poBox;
    }
    if (message.extended !== "") {
      obj.extended = message.extended;
    }
    if (message.street !== "") {
      obj.street = message.street;
    }
    if (message.locality !== "") {
      obj.locality = message.locality;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.postalCode !== "") {
      obj.postalCode = message.postalCode;
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VCardAddress>, I>>(base?: I): VCardAddress {
    return VCardAddress.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VCardAddress>, I>>(object: I): VCardAddress {
    const message = createBaseVCardAddress();
    message.type = object.type ?? 0;
    message.poBox = object.poBox ?? "";
    message.extended = object.extended ?? "";
    message.street = object.street ?? "";
    message.locality = object.locality ?? "";
    message.region = object.region ?? "";
    message.postalCode = object.postalCode ?? "";
    message.country = object.country ?? "";
    message.label = object.label ?? "";
    return message;
  },
};

function createBaseVCardTelephone(): VCardTelephone {
  return { type: 0, value: "", isPreferred: false };
}

export const VCardTelephone: MessageFns<VCardTelephone> = {
  fromJSON(object: any): VCardTelephone {
    return {
      type: isSet(object.type) ? telTypeFromJSON(object.type) : 0,
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      isPreferred: isSet(object.isPreferred)
        ? globalThis.Boolean(object.isPreferred)
        : isSet(object.is_preferred)
        ? globalThis.Boolean(object.is_preferred)
        : false,
    };
  },

  toJSON(message: VCardTelephone): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = telTypeToJSON(message.type);
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.isPreferred !== false) {
      obj.isPreferred = message.isPreferred;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VCardTelephone>, I>>(base?: I): VCardTelephone {
    return VCardTelephone.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VCardTelephone>, I>>(object: I): VCardTelephone {
    const message = createBaseVCardTelephone();
    message.type = object.type ?? 0;
    message.value = object.value ?? "";
    message.isPreferred = object.isPreferred ?? false;
    return message;
  },
};

function createBaseVCardEmail(): VCardEmail {
  return { type: 0, value: "", isPreferred: false };
}

export const VCardEmail: MessageFns<VCardEmail> = {
  fromJSON(object: any): VCardEmail {
    return {
      type: isSet(object.type) ? emailTypeFromJSON(object.type) : 0,
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      isPreferred: isSet(object.isPreferred)
        ? globalThis.Boolean(object.isPreferred)
        : isSet(object.is_preferred)
        ? globalThis.Boolean(object.is_preferred)
        : false,
    };
  },

  toJSON(message: VCardEmail): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = emailTypeToJSON(message.type);
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.isPreferred !== false) {
      obj.isPreferred = message.isPreferred;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VCardEmail>, I>>(base?: I): VCardEmail {
    return VCardEmail.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VCardEmail>, I>>(object: I): VCardEmail {
    const message = createBaseVCardEmail();
    message.type = object.type ?? 0;
    message.value = object.value ?? "";
    message.isPreferred = object.isPreferred ?? false;
    return message;
  },
};

function createBaseVCardOrganization(): VCardOrganization {
  return { name: "", unit: "" };
}

export const VCardOrganization: MessageFns<VCardOrganization> = {
  fromJSON(object: any): VCardOrganization {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      unit: isSet(object.unit) ? globalThis.String(object.unit) : "",
    };
  },

  toJSON(message: VCardOrganization): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.unit !== "") {
      obj.unit = message.unit;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VCardOrganization>, I>>(base?: I): VCardOrganization {
    return VCardOrganization.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VCardOrganization>, I>>(object: I): VCardOrganization {
    const message = createBaseVCardOrganization();
    message.name = object.name ?? "";
    message.unit = object.unit ?? "";
    return message;
  },
};

function createBaseVCardImage(): VCardImage {
  return { uri: "", mediaType: "", data: new Uint8Array(0) };
}

export const VCardImage: MessageFns<VCardImage> = {
  fromJSON(object: any): VCardImage {
    return {
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      mediaType: isSet(object.mediaType)
        ? globalThis.String(object.mediaType)
        : isSet(object.media_type)
        ? globalThis.String(object.media_type)
        : "",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: VCardImage): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.mediaType !== "") {
      obj.mediaType = message.mediaType;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VCardImage>, I>>(base?: I): VCardImage {
    return VCardImage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VCardImage>, I>>(object: I): VCardImage {
    const message = createBaseVCardImage();
    message.uri = object.uri ?? "";
    message.mediaType = object.mediaType ?? "";
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseVCardExtProperty(): VCardExtProperty {
  return { name: "", value: "" };
}

export const VCardExtProperty: MessageFns<VCardExtProperty> = {
  fromJSON(object: any): VCardExtProperty {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: VCardExtProperty): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VCardExtProperty>, I>>(base?: I): VCardExtProperty {
    return VCardExtProperty.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VCardExtProperty>, I>>(object: I): VCardExtProperty {
    const message = createBaseVCardExtProperty();
    message.name = object.name ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseInkVCard(): InkVCard {
  return {
    uid: "",
    version: 0,
    formattedName: "",
    name: undefined,
    emails: [],
    telephones: [],
    addresses: [],
    org: undefined,
    title: "",
    role: "",
    photo: undefined,
    logo: undefined,
    urls: [],
    categories: [],
    note: "",
    key: "",
    rev: "",
    isLlm: false,
    roles: [],
    folderPath: "",
    mcpUri: "",
    jcardJson: "",
    extProperties: [],
    language: "",
    timezone: "",
  };
}

export const InkVCard: MessageFns<InkVCard> = {
  fromJSON(object: any): InkVCard {
    return {
      uid: isSet(object.uid) ? globalThis.String(object.uid) : "",
      version: isSet(object.version) ? vCardVersionFromJSON(object.version) : 0,
      formattedName: isSet(object.formattedName)
        ? globalThis.String(object.formattedName)
        : isSet(object.formatted_name)
        ? globalThis.String(object.formatted_name)
        : "",
      name: isSet(object.name) ? VCardName.fromJSON(object.name) : undefined,
      emails: globalThis.Array.isArray(object?.emails)
        ? object.emails.map((e: any) => VCardEmail.fromJSON(e))
        : [],
      telephones: globalThis.Array.isArray(object?.telephones)
        ? object.telephones.map((e: any) => VCardTelephone.fromJSON(e))
        : [],
      addresses: globalThis.Array.isArray(object?.addresses)
        ? object.addresses.map((e: any) => VCardAddress.fromJSON(e))
        : [],
      org: isSet(object.org) ? VCardOrganization.fromJSON(object.org) : undefined,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      role: isSet(object.role) ? globalThis.String(object.role) : "",
      photo: isSet(object.photo) ? VCardImage.fromJSON(object.photo) : undefined,
      logo: isSet(object.logo) ? VCardImage.fromJSON(object.logo) : undefined,
      urls: globalThis.Array.isArray(object?.urls) ? object.urls.map((e: any) => globalThis.String(e)) : [],
      categories: globalThis.Array.isArray(object?.categories)
        ? object.categories.map((e: any) => globalThis.String(e))
        : [],
      note: isSet(object.note) ? globalThis.String(object.note) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      rev: isSet(object.rev) ? globalThis.String(object.rev) : "",
      isLlm: isSet(object.isLlm)
        ? globalThis.Boolean(object.isLlm)
        : isSet(object.is_llm)
        ? globalThis.Boolean(object.is_llm)
        : false,
      roles: globalThis.Array.isArray(object?.roles)
        ? object.roles.map((e: any) => globalThis.String(e))
        : [],
      folderPath: isSet(object.folderPath)
        ? globalThis.String(object.folderPath)
        : isSet(object.folder_path)
        ? globalThis.String(object.folder_path)
        : "",
      mcpUri: isSet(object.mcpUri)
        ? globalThis.String(object.mcpUri)
        : isSet(object.mcp_uri)
        ? globalThis.String(object.mcp_uri)
        : "",
      jcardJson: isSet(object.jcardJson)
        ? globalThis.String(object.jcardJson)
        : isSet(object.jcard_json)
        ? globalThis.String(object.jcard_json)
        : "",
      extProperties: globalThis.Array.isArray(object?.extProperties)
        ? object.extProperties.map((e: any) => VCardExtProperty.fromJSON(e))
        : globalThis.Array.isArray(object?.ext_properties)
        ? object.ext_properties.map((e: any) => VCardExtProperty.fromJSON(e))
        : [],
      language: isSet(object.language) ? globalThis.String(object.language) : "",
      timezone: isSet(object.timezone) ? globalThis.String(object.timezone) : "",
    };
  },

  toJSON(message: InkVCard): unknown {
    const obj: any = {};
    if (message.uid !== "") {
      obj.uid = message.uid;
    }
    if (message.version !== 0) {
      obj.version = vCardVersionToJSON(message.version);
    }
    if (message.formattedName !== "") {
      obj.formattedName = message.formattedName;
    }
    if (message.name !== undefined) {
      obj.name = VCardName.toJSON(message.name);
    }
    if (message.emails?.length) {
      obj.emails = message.emails.map((e) => VCardEmail.toJSON(e));
    }
    if (message.telephones?.length) {
      obj.telephones = message.telephones.map((e) => VCardTelephone.toJSON(e));
    }
    if (message.addresses?.length) {
      obj.addresses = message.addresses.map((e) => VCardAddress.toJSON(e));
    }
    if (message.org !== undefined) {
      obj.org = VCardOrganization.toJSON(message.org);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.role !== "") {
      obj.role = message.role;
    }
    if (message.photo !== undefined) {
      obj.photo = VCardImage.toJSON(message.photo);
    }
    if (message.logo !== undefined) {
      obj.logo = VCardImage.toJSON(message.logo);
    }
    if (message.urls?.length) {
      obj.urls = message.urls;
    }
    if (message.categories?.length) {
      obj.categories = message.categories;
    }
    if (message.note !== "") {
      obj.note = message.note;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.rev !== "") {
      obj.rev = message.rev;
    }
    if (message.isLlm !== false) {
      obj.isLlm = message.isLlm;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    if (message.folderPath !== "") {
      obj.folderPath = message.folderPath;
    }
    if (message.mcpUri !== "") {
      obj.mcpUri = message.mcpUri;
    }
    if (message.jcardJson !== "") {
      obj.jcardJson = message.jcardJson;
    }
    if (message.extProperties?.length) {
      obj.extProperties = message.extProperties.map((e) => VCardExtProperty.toJSON(e));
    }
    if (message.language !== "") {
      obj.language = message.language;
    }
    if (message.timezone !== "") {
      obj.timezone = message.timezone;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InkVCard>, I>>(base?: I): InkVCard {
    return InkVCard.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InkVCard>, I>>(object: I): InkVCard {
    const message = createBaseInkVCard();
    message.uid = object.uid ?? "";
    message.version = object.version ?? 0;
    message.formattedName = object.formattedName ?? "";
    message.name = (object.name !== undefined && object.name !== null) ? VCardName.fromPartial(object.name) : undefined;
    message.emails = object.emails?.map((e) => VCardEmail.fromPartial(e)) || [];
    message.telephones = object.telephones?.map((e) => VCardTelephone.fromPartial(e)) || [];
    message.addresses = object.addresses?.map((e) => VCardAddress.fromPartial(e)) || [];
    message.org = (object.org !== undefined && object.org !== null)
      ? VCardOrganization.fromPartial(object.org)
      : undefined;
    message.title = object.title ?? "";
    message.role = object.role ?? "";
    message.photo = (object.photo !== undefined && object.photo !== null)
      ? VCardImage.fromPartial(object.photo)
      : undefined;
    message.logo = (object.logo !== undefined && object.logo !== null)
      ? VCardImage.fromPartial(object.logo)
      : undefined;
    message.urls = object.urls?.map((e) => e) || [];
    message.categories = object.categories?.map((e) => e) || [];
    message.note = object.note ?? "";
    message.key = object.key ?? "";
    message.rev = object.rev ?? "";
    message.isLlm = object.isLlm ?? false;
    message.roles = object.roles?.map((e) => e) || [];
    message.folderPath = object.folderPath ?? "";
    message.mcpUri = object.mcpUri ?? "";
    message.jcardJson = object.jcardJson ?? "";
    message.extProperties = object.extProperties?.map((e) => VCardExtProperty.fromPartial(e)) || [];
    message.language = object.language ?? "";
    message.timezone = object.timezone ?? "";
    return message;
  },
};

function createBaseJCard(): JCard {
  return { json: "", parsed: undefined };
}

export const JCard: MessageFns<JCard> = {
  fromJSON(object: any): JCard {
    return {
      json: isSet(object.json) ? globalThis.String(object.json) : "",
      parsed: isSet(object.parsed) ? InkVCard.fromJSON(object.parsed) : undefined,
    };
  },

  toJSON(message: JCard): unknown {
    const obj: any = {};
    if (message.json !== "") {
      obj.json = message.json;
    }
    if (message.parsed !== undefined) {
      obj.parsed = InkVCard.toJSON(message.parsed);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JCard>, I>>(base?: I): JCard {
    return JCard.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JCard>, I>>(object: I): JCard {
    const message = createBaseJCard();
    message.json = object.json ?? "";
    message.parsed = (object.parsed !== undefined && object.parsed !== null)
      ? InkVCard.fromPartial(object.parsed)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
