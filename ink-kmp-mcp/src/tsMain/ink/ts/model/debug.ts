// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v5.28.3
// source: ink/model/debug.proto

/* eslint-disable */
import { Choice } from "./story";

export const protobufPackage = "ink.model";

export interface Breakpoint {
  id: string;
  /** knot, stitch, pattern, variable_change */
  type: string;
  target: string;
  enabled: boolean;
}

export interface WatchVariable {
  name: string;
  lastValue: string;
  changeCount: number;
}

export interface VisitEntry {
  step: number;
  text: string;
  choicesMade: number;
  variablesChanged: string[];
  timestamp: number;
}

export interface DebugSession {
  sessionId: string;
  breakpoints: Breakpoint[];
  watches: { [key: string]: WatchVariable };
  visitLog: VisitEntry[];
  stepping: boolean;
  stepCount: number;
  totalSteps: number;
  isPaused: boolean;
  lastOutput: string;
}

export interface DebugSession_WatchesEntry {
  key: string;
  value: WatchVariable | undefined;
}

export interface WatchChange {
  oldValue: string;
  newValue: string;
}

export interface StepResult {
  text: string;
  canContinue: boolean;
  choices: Choice[];
  tags: string[];
  hitBreakpoint: Breakpoint | undefined;
  watchChanges: { [key: string]: WatchChange };
  stepNumber: number;
  isPaused: boolean;
}

export interface StepResult_WatchChangesEntry {
  key: string;
  value: WatchChange | undefined;
}

function createBaseBreakpoint(): Breakpoint {
  return { id: "", type: "", target: "", enabled: false };
}

export const Breakpoint: MessageFns<Breakpoint> = {
  fromJSON(object: any): Breakpoint {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: Breakpoint): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Breakpoint>, I>>(base?: I): Breakpoint {
    return Breakpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Breakpoint>, I>>(object: I): Breakpoint {
    const message = createBaseBreakpoint();
    message.id = object.id ?? "";
    message.type = object.type ?? "";
    message.target = object.target ?? "";
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseWatchVariable(): WatchVariable {
  return { name: "", lastValue: "", changeCount: 0 };
}

export const WatchVariable: MessageFns<WatchVariable> = {
  fromJSON(object: any): WatchVariable {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      lastValue: isSet(object.lastValue)
        ? globalThis.String(object.lastValue)
        : isSet(object.last_value)
        ? globalThis.String(object.last_value)
        : "",
      changeCount: isSet(object.changeCount)
        ? globalThis.Number(object.changeCount)
        : isSet(object.change_count)
        ? globalThis.Number(object.change_count)
        : 0,
    };
  },

  toJSON(message: WatchVariable): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.lastValue !== "") {
      obj.lastValue = message.lastValue;
    }
    if (message.changeCount !== 0) {
      obj.changeCount = Math.round(message.changeCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WatchVariable>, I>>(base?: I): WatchVariable {
    return WatchVariable.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WatchVariable>, I>>(object: I): WatchVariable {
    const message = createBaseWatchVariable();
    message.name = object.name ?? "";
    message.lastValue = object.lastValue ?? "";
    message.changeCount = object.changeCount ?? 0;
    return message;
  },
};

function createBaseVisitEntry(): VisitEntry {
  return { step: 0, text: "", choicesMade: 0, variablesChanged: [], timestamp: 0 };
}

export const VisitEntry: MessageFns<VisitEntry> = {
  fromJSON(object: any): VisitEntry {
    return {
      step: isSet(object.step) ? globalThis.Number(object.step) : 0,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      choicesMade: isSet(object.choicesMade)
        ? globalThis.Number(object.choicesMade)
        : isSet(object.choices_made)
        ? globalThis.Number(object.choices_made)
        : 0,
      variablesChanged: globalThis.Array.isArray(object?.variablesChanged)
        ? object.variablesChanged.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.variables_changed)
        ? object.variables_changed.map((e: any) => globalThis.String(e))
        : [],
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: VisitEntry): unknown {
    const obj: any = {};
    if (message.step !== 0) {
      obj.step = Math.round(message.step);
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.choicesMade !== 0) {
      obj.choicesMade = Math.round(message.choicesMade);
    }
    if (message.variablesChanged?.length) {
      obj.variablesChanged = message.variablesChanged;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VisitEntry>, I>>(base?: I): VisitEntry {
    return VisitEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VisitEntry>, I>>(object: I): VisitEntry {
    const message = createBaseVisitEntry();
    message.step = object.step ?? 0;
    message.text = object.text ?? "";
    message.choicesMade = object.choicesMade ?? 0;
    message.variablesChanged = object.variablesChanged?.map((e) => e) || [];
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseDebugSession(): DebugSession {
  return {
    sessionId: "",
    breakpoints: [],
    watches: {},
    visitLog: [],
    stepping: false,
    stepCount: 0,
    totalSteps: 0,
    isPaused: false,
    lastOutput: "",
  };
}

export const DebugSession: MessageFns<DebugSession> = {
  fromJSON(object: any): DebugSession {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      breakpoints: globalThis.Array.isArray(object?.breakpoints)
        ? object.breakpoints.map((e: any) => Breakpoint.fromJSON(e))
        : [],
      watches: isObject(object.watches)
        ? (globalThis.Object.entries(object.watches) as [string, any][]).reduce(
          (acc: { [key: string]: WatchVariable }, [key, value]: [string, any]) => {
            acc[key] = WatchVariable.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      visitLog: globalThis.Array.isArray(object?.visitLog)
        ? object.visitLog.map((e: any) => VisitEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.visit_log)
        ? object.visit_log.map((e: any) => VisitEntry.fromJSON(e))
        : [],
      stepping: isSet(object.stepping) ? globalThis.Boolean(object.stepping) : false,
      stepCount: isSet(object.stepCount)
        ? globalThis.Number(object.stepCount)
        : isSet(object.step_count)
        ? globalThis.Number(object.step_count)
        : 0,
      totalSteps: isSet(object.totalSteps)
        ? globalThis.Number(object.totalSteps)
        : isSet(object.total_steps)
        ? globalThis.Number(object.total_steps)
        : 0,
      isPaused: isSet(object.isPaused)
        ? globalThis.Boolean(object.isPaused)
        : isSet(object.is_paused)
        ? globalThis.Boolean(object.is_paused)
        : false,
      lastOutput: isSet(object.lastOutput)
        ? globalThis.String(object.lastOutput)
        : isSet(object.last_output)
        ? globalThis.String(object.last_output)
        : "",
    };
  },

  toJSON(message: DebugSession): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.breakpoints?.length) {
      obj.breakpoints = message.breakpoints.map((e) => Breakpoint.toJSON(e));
    }
    if (message.watches) {
      const entries = globalThis.Object.entries(message.watches) as [string, WatchVariable][];
      if (entries.length > 0) {
        obj.watches = {};
        entries.forEach(([k, v]) => {
          obj.watches[k] = WatchVariable.toJSON(v);
        });
      }
    }
    if (message.visitLog?.length) {
      obj.visitLog = message.visitLog.map((e) => VisitEntry.toJSON(e));
    }
    if (message.stepping !== false) {
      obj.stepping = message.stepping;
    }
    if (message.stepCount !== 0) {
      obj.stepCount = Math.round(message.stepCount);
    }
    if (message.totalSteps !== 0) {
      obj.totalSteps = Math.round(message.totalSteps);
    }
    if (message.isPaused !== false) {
      obj.isPaused = message.isPaused;
    }
    if (message.lastOutput !== "") {
      obj.lastOutput = message.lastOutput;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DebugSession>, I>>(base?: I): DebugSession {
    return DebugSession.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DebugSession>, I>>(object: I): DebugSession {
    const message = createBaseDebugSession();
    message.sessionId = object.sessionId ?? "";
    message.breakpoints = object.breakpoints?.map((e) => Breakpoint.fromPartial(e)) || [];
    message.watches = (globalThis.Object.entries(object.watches ?? {}) as [string, WatchVariable][]).reduce(
      (acc: { [key: string]: WatchVariable }, [key, value]: [string, WatchVariable]) => {
        if (value !== undefined) {
          acc[key] = WatchVariable.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.visitLog = object.visitLog?.map((e) => VisitEntry.fromPartial(e)) || [];
    message.stepping = object.stepping ?? false;
    message.stepCount = object.stepCount ?? 0;
    message.totalSteps = object.totalSteps ?? 0;
    message.isPaused = object.isPaused ?? false;
    message.lastOutput = object.lastOutput ?? "";
    return message;
  },
};

function createBaseDebugSession_WatchesEntry(): DebugSession_WatchesEntry {
  return { key: "", value: undefined };
}

export const DebugSession_WatchesEntry: MessageFns<DebugSession_WatchesEntry> = {
  fromJSON(object: any): DebugSession_WatchesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? WatchVariable.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: DebugSession_WatchesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = WatchVariable.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DebugSession_WatchesEntry>, I>>(base?: I): DebugSession_WatchesEntry {
    return DebugSession_WatchesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DebugSession_WatchesEntry>, I>>(object: I): DebugSession_WatchesEntry {
    const message = createBaseDebugSession_WatchesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? WatchVariable.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseWatchChange(): WatchChange {
  return { oldValue: "", newValue: "" };
}

export const WatchChange: MessageFns<WatchChange> = {
  fromJSON(object: any): WatchChange {
    return {
      oldValue: isSet(object.oldValue)
        ? globalThis.String(object.oldValue)
        : isSet(object.old_value)
        ? globalThis.String(object.old_value)
        : "",
      newValue: isSet(object.newValue)
        ? globalThis.String(object.newValue)
        : isSet(object.new_value)
        ? globalThis.String(object.new_value)
        : "",
    };
  },

  toJSON(message: WatchChange): unknown {
    const obj: any = {};
    if (message.oldValue !== "") {
      obj.oldValue = message.oldValue;
    }
    if (message.newValue !== "") {
      obj.newValue = message.newValue;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WatchChange>, I>>(base?: I): WatchChange {
    return WatchChange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WatchChange>, I>>(object: I): WatchChange {
    const message = createBaseWatchChange();
    message.oldValue = object.oldValue ?? "";
    message.newValue = object.newValue ?? "";
    return message;
  },
};

function createBaseStepResult(): StepResult {
  return {
    text: "",
    canContinue: false,
    choices: [],
    tags: [],
    hitBreakpoint: undefined,
    watchChanges: {},
    stepNumber: 0,
    isPaused: false,
  };
}

export const StepResult: MessageFns<StepResult> = {
  fromJSON(object: any): StepResult {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      canContinue: isSet(object.canContinue)
        ? globalThis.Boolean(object.canContinue)
        : isSet(object.can_continue)
        ? globalThis.Boolean(object.can_continue)
        : false,
      choices: globalThis.Array.isArray(object?.choices)
        ? object.choices.map((e: any) => Choice.fromJSON(e))
        : [],
      tags: globalThis.Array.isArray(object?.tags)
        ? object.tags.map((e: any) => globalThis.String(e))
        : [],
      hitBreakpoint: isSet(object.hitBreakpoint)
        ? Breakpoint.fromJSON(object.hitBreakpoint)
        : isSet(object.hit_breakpoint)
        ? Breakpoint.fromJSON(object.hit_breakpoint)
        : undefined,
      watchChanges: isObject(object.watchChanges)
        ? (globalThis.Object.entries(object.watchChanges) as [string, any][]).reduce(
          (acc: { [key: string]: WatchChange }, [key, value]: [string, any]) => {
            acc[key] = WatchChange.fromJSON(value);
            return acc;
          },
          {},
        )
        : isObject(object.watch_changes)
        ? (globalThis.Object.entries(object.watch_changes) as [string, any][]).reduce(
          (acc: { [key: string]: WatchChange }, [key, value]: [string, any]) => {
            acc[key] = WatchChange.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      stepNumber: isSet(object.stepNumber)
        ? globalThis.Number(object.stepNumber)
        : isSet(object.step_number)
        ? globalThis.Number(object.step_number)
        : 0,
      isPaused: isSet(object.isPaused)
        ? globalThis.Boolean(object.isPaused)
        : isSet(object.is_paused)
        ? globalThis.Boolean(object.is_paused)
        : false,
    };
  },

  toJSON(message: StepResult): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.canContinue !== false) {
      obj.canContinue = message.canContinue;
    }
    if (message.choices?.length) {
      obj.choices = message.choices.map((e) => Choice.toJSON(e));
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.hitBreakpoint !== undefined) {
      obj.hitBreakpoint = Breakpoint.toJSON(message.hitBreakpoint);
    }
    if (message.watchChanges) {
      const entries = globalThis.Object.entries(message.watchChanges) as [string, WatchChange][];
      if (entries.length > 0) {
        obj.watchChanges = {};
        entries.forEach(([k, v]) => {
          obj.watchChanges[k] = WatchChange.toJSON(v);
        });
      }
    }
    if (message.stepNumber !== 0) {
      obj.stepNumber = Math.round(message.stepNumber);
    }
    if (message.isPaused !== false) {
      obj.isPaused = message.isPaused;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StepResult>, I>>(base?: I): StepResult {
    return StepResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StepResult>, I>>(object: I): StepResult {
    const message = createBaseStepResult();
    message.text = object.text ?? "";
    message.canContinue = object.canContinue ?? false;
    message.choices = object.choices?.map((e) => Choice.fromPartial(e)) || [];
    message.tags = object.tags?.map((e) => e) || [];
    message.hitBreakpoint = (object.hitBreakpoint !== undefined && object.hitBreakpoint !== null)
      ? Breakpoint.fromPartial(object.hitBreakpoint)
      : undefined;
    message.watchChanges = (globalThis.Object.entries(object.watchChanges ?? {}) as [string, WatchChange][]).reduce(
      (acc: { [key: string]: WatchChange }, [key, value]: [string, WatchChange]) => {
        if (value !== undefined) {
          acc[key] = WatchChange.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.stepNumber = object.stepNumber ?? 0;
    message.isPaused = object.isPaused ?? false;
    return message;
  },
};

function createBaseStepResult_WatchChangesEntry(): StepResult_WatchChangesEntry {
  return { key: "", value: undefined };
}

export const StepResult_WatchChangesEntry: MessageFns<StepResult_WatchChangesEntry> = {
  fromJSON(object: any): StepResult_WatchChangesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? WatchChange.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: StepResult_WatchChangesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = WatchChange.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StepResult_WatchChangesEntry>, I>>(base?: I): StepResult_WatchChangesEntry {
    return StepResult_WatchChangesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StepResult_WatchChangesEntry>, I>>(object: I): StepResult_WatchChangesEntry {
    const message = createBaseStepResult_WatchChangesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? WatchChange.fromPartial(object.value)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
