// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v5.28.3
// source: ink/model/collab.proto

/* eslint-disable */

export const protobufPackage = "ink.model";

export interface ColabClient {
  clientId: string;
  docId: string;
}

export interface ColabDocumentInfo {
  docId: string;
  createdAt: number;
  updateCount: number;
  clientCount: number;
}

export interface EditorContext {
  /** "edit" or "play" */
  mode: string;
  sessionId: string;
  docId: string;
  isCompiling: boolean;
  errors: string[];
}

function createBaseColabClient(): ColabClient {
  return { clientId: "", docId: "" };
}

export const ColabClient: MessageFns<ColabClient> = {
  fromJSON(object: any): ColabClient {
    return {
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      docId: isSet(object.docId)
        ? globalThis.String(object.docId)
        : isSet(object.doc_id)
        ? globalThis.String(object.doc_id)
        : "",
    };
  },

  toJSON(message: ColabClient): unknown {
    const obj: any = {};
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.docId !== "") {
      obj.docId = message.docId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ColabClient>, I>>(base?: I): ColabClient {
    return ColabClient.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ColabClient>, I>>(object: I): ColabClient {
    const message = createBaseColabClient();
    message.clientId = object.clientId ?? "";
    message.docId = object.docId ?? "";
    return message;
  },
};

function createBaseColabDocumentInfo(): ColabDocumentInfo {
  return { docId: "", createdAt: 0, updateCount: 0, clientCount: 0 };
}

export const ColabDocumentInfo: MessageFns<ColabDocumentInfo> = {
  fromJSON(object: any): ColabDocumentInfo {
    return {
      docId: isSet(object.docId)
        ? globalThis.String(object.docId)
        : isSet(object.doc_id)
        ? globalThis.String(object.doc_id)
        : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.Number(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.Number(object.created_at)
        : 0,
      updateCount: isSet(object.updateCount)
        ? globalThis.Number(object.updateCount)
        : isSet(object.update_count)
        ? globalThis.Number(object.update_count)
        : 0,
      clientCount: isSet(object.clientCount)
        ? globalThis.Number(object.clientCount)
        : isSet(object.client_count)
        ? globalThis.Number(object.client_count)
        : 0,
    };
  },

  toJSON(message: ColabDocumentInfo): unknown {
    const obj: any = {};
    if (message.docId !== "") {
      obj.docId = message.docId;
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.updateCount !== 0) {
      obj.updateCount = Math.round(message.updateCount);
    }
    if (message.clientCount !== 0) {
      obj.clientCount = Math.round(message.clientCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ColabDocumentInfo>, I>>(base?: I): ColabDocumentInfo {
    return ColabDocumentInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ColabDocumentInfo>, I>>(object: I): ColabDocumentInfo {
    const message = createBaseColabDocumentInfo();
    message.docId = object.docId ?? "";
    message.createdAt = object.createdAt ?? 0;
    message.updateCount = object.updateCount ?? 0;
    message.clientCount = object.clientCount ?? 0;
    return message;
  },
};

function createBaseEditorContext(): EditorContext {
  return { mode: "", sessionId: "", docId: "", isCompiling: false, errors: [] };
}

export const EditorContext: MessageFns<EditorContext> = {
  fromJSON(object: any): EditorContext {
    return {
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      docId: isSet(object.docId)
        ? globalThis.String(object.docId)
        : isSet(object.doc_id)
        ? globalThis.String(object.doc_id)
        : "",
      isCompiling: isSet(object.isCompiling)
        ? globalThis.Boolean(object.isCompiling)
        : isSet(object.is_compiling)
        ? globalThis.Boolean(object.is_compiling)
        : false,
      errors: globalThis.Array.isArray(object?.errors)
        ? object.errors.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: EditorContext): unknown {
    const obj: any = {};
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.docId !== "") {
      obj.docId = message.docId;
    }
    if (message.isCompiling !== false) {
      obj.isCompiling = message.isCompiling;
    }
    if (message.errors?.length) {
      obj.errors = message.errors;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EditorContext>, I>>(base?: I): EditorContext {
    return EditorContext.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EditorContext>, I>>(object: I): EditorContext {
    const message = createBaseEditorContext();
    message.mode = object.mode ?? "";
    message.sessionId = object.sessionId ?? "";
    message.docId = object.docId ?? "";
    message.isCompiling = object.isCompiling ?? false;
    message.errors = object.errors?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
