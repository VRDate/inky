// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v5.28.3
// source: ink/model/structure.proto

/* eslint-disable */

export const protobufPackage = "ink.model";

export enum SectionType {
  SECTION_TYPE_UNSPECIFIED = 0,
  KNOT = 1,
  STITCH = 2,
  FUNCTION = 3,
  PREAMBLE = 4,
  UNRECOGNIZED = -1,
}

export function sectionTypeFromJSON(object: any): SectionType {
  switch (object) {
    case 0:
    case "SECTION_TYPE_UNSPECIFIED":
      return SectionType.SECTION_TYPE_UNSPECIFIED;
    case 1:
    case "KNOT":
      return SectionType.KNOT;
    case 2:
    case "STITCH":
      return SectionType.STITCH;
    case 3:
    case "FUNCTION":
      return SectionType.FUNCTION;
    case 4:
    case "PREAMBLE":
      return SectionType.PREAMBLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SectionType.UNRECOGNIZED;
  }
}

export function sectionTypeToJSON(object: SectionType): string {
  switch (object) {
    case SectionType.SECTION_TYPE_UNSPECIFIED:
      return "SECTION_TYPE_UNSPECIFIED";
    case SectionType.KNOT:
      return "KNOT";
    case SectionType.STITCH:
      return "STITCH";
    case SectionType.FUNCTION:
      return "FUNCTION";
    case SectionType.PREAMBLE:
      return "PREAMBLE";
    case SectionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum VariableType {
  VARIABLE_TYPE_UNSPECIFIED = 0,
  VAR = 1,
  CONST = 2,
  LIST = 3,
  TEMP = 4,
  UNRECOGNIZED = -1,
}

export function variableTypeFromJSON(object: any): VariableType {
  switch (object) {
    case 0:
    case "VARIABLE_TYPE_UNSPECIFIED":
      return VariableType.VARIABLE_TYPE_UNSPECIFIED;
    case 1:
    case "VAR":
      return VariableType.VAR;
    case 2:
    case "CONST":
      return VariableType.CONST;
    case 3:
    case "LIST":
      return VariableType.LIST;
    case 4:
    case "TEMP":
      return VariableType.TEMP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return VariableType.UNRECOGNIZED;
  }
}

export function variableTypeToJSON(object: VariableType): string {
  switch (object) {
    case VariableType.VARIABLE_TYPE_UNSPECIFIED:
      return "VARIABLE_TYPE_UNSPECIFIED";
    case VariableType.VAR:
      return "VAR";
    case VariableType.CONST:
      return "CONST";
    case VariableType.LIST:
      return "LIST";
    case VariableType.TEMP:
      return "TEMP";
    case VariableType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum DiagramMode {
  DIAGRAM_MODE_UNSPECIFIED = 0,
  ACTIVITY = 1,
  STATE = 2,
  UNRECOGNIZED = -1,
}

export function diagramModeFromJSON(object: any): DiagramMode {
  switch (object) {
    case 0:
    case "DIAGRAM_MODE_UNSPECIFIED":
      return DiagramMode.DIAGRAM_MODE_UNSPECIFIED;
    case 1:
    case "ACTIVITY":
      return DiagramMode.ACTIVITY;
    case 2:
    case "STATE":
      return DiagramMode.STATE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DiagramMode.UNRECOGNIZED;
  }
}

export function diagramModeToJSON(object: DiagramMode): string {
  switch (object) {
    case DiagramMode.DIAGRAM_MODE_UNSPECIFIED:
      return "DIAGRAM_MODE_UNSPECIFIED";
    case DiagramMode.ACTIVITY:
      return "ACTIVITY";
    case DiagramMode.STATE:
      return "STATE";
    case DiagramMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Section {
  name: string;
  type: SectionType;
  startLine: number;
  endLine: number;
  content: string;
  parent: string;
  parameters: string[];
  /** end_line - start_line */
  lineCount: number;
}

export interface Variable {
  name: string;
  type: VariableType;
  initialValue: string;
  line: number;
}

export interface DivertRef {
  target: string;
  line: number;
  column: number;
}

export interface Structure {
  sections: Section[];
  variables: Variable[];
  includes: string[];
  diverts: DivertRef[];
  totalLines: number;
  /** len(diverts) — convenience field */
  divertCount: number;
}

/** Ink choice from Ink2PumlEngine */
export interface InkDiagramChoice {
  text: string;
  divert: string;
  isStickyChoice: boolean;
}

function createBaseSection(): Section {
  return { name: "", type: 0, startLine: 0, endLine: 0, content: "", parent: "", parameters: [], lineCount: 0 };
}

export const Section: MessageFns<Section> = {
  fromJSON(object: any): Section {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? sectionTypeFromJSON(object.type) : 0,
      startLine: isSet(object.startLine)
        ? globalThis.Number(object.startLine)
        : isSet(object.start_line)
        ? globalThis.Number(object.start_line)
        : 0,
      endLine: isSet(object.endLine)
        ? globalThis.Number(object.endLine)
        : isSet(object.end_line)
        ? globalThis.Number(object.end_line)
        : 0,
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      parameters: globalThis.Array.isArray(object?.parameters)
        ? object.parameters.map((e: any) => globalThis.String(e))
        : [],
      lineCount: isSet(object.lineCount)
        ? globalThis.Number(object.lineCount)
        : isSet(object.line_count)
        ? globalThis.Number(object.line_count)
        : 0,
    };
  },

  toJSON(message: Section): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== 0) {
      obj.type = sectionTypeToJSON(message.type);
    }
    if (message.startLine !== 0) {
      obj.startLine = Math.round(message.startLine);
    }
    if (message.endLine !== 0) {
      obj.endLine = Math.round(message.endLine);
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.parameters?.length) {
      obj.parameters = message.parameters;
    }
    if (message.lineCount !== 0) {
      obj.lineCount = Math.round(message.lineCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Section>, I>>(base?: I): Section {
    return Section.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Section>, I>>(object: I): Section {
    const message = createBaseSection();
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    message.startLine = object.startLine ?? 0;
    message.endLine = object.endLine ?? 0;
    message.content = object.content ?? "";
    message.parent = object.parent ?? "";
    message.parameters = object.parameters?.map((e) => e) || [];
    message.lineCount = object.lineCount ?? 0;
    return message;
  },
};

function createBaseVariable(): Variable {
  return { name: "", type: 0, initialValue: "", line: 0 };
}

export const Variable: MessageFns<Variable> = {
  fromJSON(object: any): Variable {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? variableTypeFromJSON(object.type) : 0,
      initialValue: isSet(object.initialValue)
        ? globalThis.String(object.initialValue)
        : isSet(object.initial_value)
        ? globalThis.String(object.initial_value)
        : "",
      line: isSet(object.line) ? globalThis.Number(object.line) : 0,
    };
  },

  toJSON(message: Variable): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== 0) {
      obj.type = variableTypeToJSON(message.type);
    }
    if (message.initialValue !== "") {
      obj.initialValue = message.initialValue;
    }
    if (message.line !== 0) {
      obj.line = Math.round(message.line);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Variable>, I>>(base?: I): Variable {
    return Variable.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Variable>, I>>(object: I): Variable {
    const message = createBaseVariable();
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    message.initialValue = object.initialValue ?? "";
    message.line = object.line ?? 0;
    return message;
  },
};

function createBaseDivertRef(): DivertRef {
  return { target: "", line: 0, column: 0 };
}

export const DivertRef: MessageFns<DivertRef> = {
  fromJSON(object: any): DivertRef {
    return {
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      line: isSet(object.line) ? globalThis.Number(object.line) : 0,
      column: isSet(object.column) ? globalThis.Number(object.column) : 0,
    };
  },

  toJSON(message: DivertRef): unknown {
    const obj: any = {};
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.line !== 0) {
      obj.line = Math.round(message.line);
    }
    if (message.column !== 0) {
      obj.column = Math.round(message.column);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DivertRef>, I>>(base?: I): DivertRef {
    return DivertRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DivertRef>, I>>(object: I): DivertRef {
    const message = createBaseDivertRef();
    message.target = object.target ?? "";
    message.line = object.line ?? 0;
    message.column = object.column ?? 0;
    return message;
  },
};

function createBaseStructure(): Structure {
  return { sections: [], variables: [], includes: [], diverts: [], totalLines: 0, divertCount: 0 };
}

export const Structure: MessageFns<Structure> = {
  fromJSON(object: any): Structure {
    return {
      sections: globalThis.Array.isArray(object?.sections) ? object.sections.map((e: any) => Section.fromJSON(e)) : [],
      variables: globalThis.Array.isArray(object?.variables)
        ? object.variables.map((e: any) => Variable.fromJSON(e))
        : [],
      includes: globalThis.Array.isArray(object?.includes) ? object.includes.map((e: any) => globalThis.String(e)) : [],
      diverts: globalThis.Array.isArray(object?.diverts) ? object.diverts.map((e: any) => DivertRef.fromJSON(e)) : [],
      totalLines: isSet(object.totalLines)
        ? globalThis.Number(object.totalLines)
        : isSet(object.total_lines)
        ? globalThis.Number(object.total_lines)
        : 0,
      divertCount: isSet(object.divertCount)
        ? globalThis.Number(object.divertCount)
        : isSet(object.divert_count)
        ? globalThis.Number(object.divert_count)
        : 0,
    };
  },

  toJSON(message: Structure): unknown {
    const obj: any = {};
    if (message.sections?.length) {
      obj.sections = message.sections.map((e) => Section.toJSON(e));
    }
    if (message.variables?.length) {
      obj.variables = message.variables.map((e) => Variable.toJSON(e));
    }
    if (message.includes?.length) {
      obj.includes = message.includes;
    }
    if (message.diverts?.length) {
      obj.diverts = message.diverts.map((e) => DivertRef.toJSON(e));
    }
    if (message.totalLines !== 0) {
      obj.totalLines = Math.round(message.totalLines);
    }
    if (message.divertCount !== 0) {
      obj.divertCount = Math.round(message.divertCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Structure>, I>>(base?: I): Structure {
    return Structure.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Structure>, I>>(object: I): Structure {
    const message = createBaseStructure();
    message.sections = object.sections?.map((e) => Section.fromPartial(e)) || [];
    message.variables = object.variables?.map((e) => Variable.fromPartial(e)) || [];
    message.includes = object.includes?.map((e) => e) || [];
    message.diverts = object.diverts?.map((e) => DivertRef.fromPartial(e)) || [];
    message.totalLines = object.totalLines ?? 0;
    message.divertCount = object.divertCount ?? 0;
    return message;
  },
};

function createBaseInkDiagramChoice(): InkDiagramChoice {
  return { text: "", divert: "", isStickyChoice: false };
}

export const InkDiagramChoice: MessageFns<InkDiagramChoice> = {
  fromJSON(object: any): InkDiagramChoice {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      divert: isSet(object.divert) ? globalThis.String(object.divert) : "",
      isStickyChoice: isSet(object.isStickyChoice)
        ? globalThis.Boolean(object.isStickyChoice)
        : isSet(object.is_sticky_choice)
        ? globalThis.Boolean(object.is_sticky_choice)
        : false,
    };
  },

  toJSON(message: InkDiagramChoice): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.divert !== "") {
      obj.divert = message.divert;
    }
    if (message.isStickyChoice !== false) {
      obj.isStickyChoice = message.isStickyChoice;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InkDiagramChoice>, I>>(base?: I): InkDiagramChoice {
    return InkDiagramChoice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InkDiagramChoice>, I>>(object: I): InkDiagramChoice {
    const message = createBaseInkDiagramChoice();
    message.text = object.text ?? "";
    message.divert = object.divert ?? "";
    message.isStickyChoice = object.isStickyChoice ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
