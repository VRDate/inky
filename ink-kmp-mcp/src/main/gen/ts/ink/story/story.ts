// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v5.28.3
// source: ink/story/story.proto

/* eslint-disable */

export const protobufPackage = "ink.story.model";

export interface Choice {
  index: number;
  text: string;
  tags: string[];
}

export interface StoryState {
  text: string;
  canContinue: boolean;
  choices: Choice[];
  tags: string[];
}

export interface CompileResult {
  success: boolean;
  json: string;
  errors: string[];
  warnings: string[];
}

export interface StorySession {
  id: string;
  source: string;
  stateJson: string;
}

function createBaseChoice(): Choice {
  return { index: 0, text: "", tags: [] };
}

export const Choice: MessageFns<Choice> = {
  fromJSON(object: any): Choice {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: Choice): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Choice>, I>>(base?: I): Choice {
    return Choice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Choice>, I>>(object: I): Choice {
    const message = createBaseChoice();
    message.index = object.index ?? 0;
    message.text = object.text ?? "";
    message.tags = object.tags?.map((e) => e) || [];
    return message;
  },
};

function createBaseStoryState(): StoryState {
  return { text: "", canContinue: false, choices: [], tags: [] };
}

export const StoryState: MessageFns<StoryState> = {
  fromJSON(object: any): StoryState {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      canContinue: isSet(object.canContinue)
        ? globalThis.Boolean(object.canContinue)
        : isSet(object.can_continue)
        ? globalThis.Boolean(object.can_continue)
        : false,
      choices: globalThis.Array.isArray(object?.choices)
        ? object.choices.map((e: any) => Choice.fromJSON(e))
        : [],
      tags: globalThis.Array.isArray(object?.tags)
        ? object.tags.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: StoryState): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.canContinue !== false) {
      obj.canContinue = message.canContinue;
    }
    if (message.choices?.length) {
      obj.choices = message.choices.map((e) => Choice.toJSON(e));
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoryState>, I>>(base?: I): StoryState {
    return StoryState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoryState>, I>>(object: I): StoryState {
    const message = createBaseStoryState();
    message.text = object.text ?? "";
    message.canContinue = object.canContinue ?? false;
    message.choices = object.choices?.map((e) => Choice.fromPartial(e)) || [];
    message.tags = object.tags?.map((e) => e) || [];
    return message;
  },
};

function createBaseCompileResult(): CompileResult {
  return { success: false, json: "", errors: [], warnings: [] };
}

export const CompileResult: MessageFns<CompileResult> = {
  fromJSON(object: any): CompileResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      json: isSet(object.json) ? globalThis.String(object.json) : "",
      errors: globalThis.Array.isArray(object?.errors) ? object.errors.map((e: any) => globalThis.String(e)) : [],
      warnings: globalThis.Array.isArray(object?.warnings) ? object.warnings.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: CompileResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.json !== "") {
      obj.json = message.json;
    }
    if (message.errors?.length) {
      obj.errors = message.errors;
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CompileResult>, I>>(base?: I): CompileResult {
    return CompileResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CompileResult>, I>>(object: I): CompileResult {
    const message = createBaseCompileResult();
    message.success = object.success ?? false;
    message.json = object.json ?? "";
    message.errors = object.errors?.map((e) => e) || [];
    message.warnings = object.warnings?.map((e) => e) || [];
    return message;
  },
};

function createBaseStorySession(): StorySession {
  return { id: "", source: "", stateJson: "" };
}

export const StorySession: MessageFns<StorySession> = {
  fromJSON(object: any): StorySession {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      stateJson: isSet(object.stateJson)
        ? globalThis.String(object.stateJson)
        : isSet(object.state_json)
        ? globalThis.String(object.state_json)
        : "",
    };
  },

  toJSON(message: StorySession): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.stateJson !== "") {
      obj.stateJson = message.stateJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorySession>, I>>(base?: I): StorySession {
    return StorySession.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorySession>, I>>(object: I): StorySession {
    const message = createBaseStorySession();
    message.id = object.id ?? "";
    message.source = object.source ?? "";
    message.stateJson = object.stateJson ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
