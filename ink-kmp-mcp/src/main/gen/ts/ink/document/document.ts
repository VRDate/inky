// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v5.28.3
// source: ink/document/document.proto

/* eslint-disable */
import { MdTable } from "../table/table";

export const protobufPackage = "ink.document.model";

export enum EditorMode {
  EDITOR_MODE_UNSPECIFIED = 0,
  EDIT = 1,
  PLAY = 2,
  UNRECOGNIZED = -1,
}

export function editorModeFromJSON(object: any): EditorMode {
  switch (object) {
    case 0:
    case "EDITOR_MODE_UNSPECIFIED":
      return EditorMode.EDITOR_MODE_UNSPECIFIED;
    case 1:
    case "EDIT":
      return EditorMode.EDIT;
    case 2:
    case "PLAY":
      return EditorMode.PLAY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EditorMode.UNRECOGNIZED;
  }
}

export function editorModeToJSON(object: EditorMode): string {
  switch (object) {
    case EditorMode.EDITOR_MODE_UNSPECIFIED:
      return "EDITOR_MODE_UNSPECIFIED";
    case EditorMode.EDIT:
      return "EDIT";
    case EditorMode.PLAY:
      return "PLAY";
    case EditorMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface InkFile {
  name: string;
  inkSource: string;
  headerLevel: number;
}

export interface ParseResult {
  files: InkFile[];
  tables: MdTable[];
}

function createBaseInkFile(): InkFile {
  return { name: "", inkSource: "", headerLevel: 0 };
}

export const InkFile: MessageFns<InkFile> = {
  fromJSON(object: any): InkFile {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      inkSource: isSet(object.inkSource)
        ? globalThis.String(object.inkSource)
        : isSet(object.ink_source)
        ? globalThis.String(object.ink_source)
        : "",
      headerLevel: isSet(object.headerLevel)
        ? globalThis.Number(object.headerLevel)
        : isSet(object.header_level)
        ? globalThis.Number(object.header_level)
        : 0,
    };
  },

  toJSON(message: InkFile): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.inkSource !== "") {
      obj.inkSource = message.inkSource;
    }
    if (message.headerLevel !== 0) {
      obj.headerLevel = Math.round(message.headerLevel);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InkFile>, I>>(base?: I): InkFile {
    return InkFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InkFile>, I>>(object: I): InkFile {
    const message = createBaseInkFile();
    message.name = object.name ?? "";
    message.inkSource = object.inkSource ?? "";
    message.headerLevel = object.headerLevel ?? 0;
    return message;
  },
};

function createBaseParseResult(): ParseResult {
  return { files: [], tables: [] };
}

export const ParseResult: MessageFns<ParseResult> = {
  fromJSON(object: any): ParseResult {
    return {
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => InkFile.fromJSON(e)) : [],
      tables: globalThis.Array.isArray(object?.tables) ? object.tables.map((e: any) => MdTable.fromJSON(e)) : [],
    };
  },

  toJSON(message: ParseResult): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => InkFile.toJSON(e));
    }
    if (message.tables?.length) {
      obj.tables = message.tables.map((e) => MdTable.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParseResult>, I>>(base?: I): ParseResult {
    return ParseResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParseResult>, I>>(object: I): ParseResult {
    const message = createBaseParseResult();
    message.files = object.files?.map((e) => InkFile.fromPartial(e)) || [];
    message.tables = object.tables?.map((e) => MdTable.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
