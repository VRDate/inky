package ink.kt

/**
 * Severity level for runtime errors and warnings.
 *
 * Three-way comparison:
 * - C#: `Ink.Runtime.ErrorType` — enum { Author, Warning, Error }
 * - Java: `Error` — enum (name collision with java.lang.Error, confusing)
 * - JS: `ErrorType` — same as C#
 *
 * Kotlin improvements:
 * - **Named `ErrorType`** — matches C#, avoids Java's `Error` name collision
 * - **`fun interface ErrorHandler`** — SAM interface for error callbacks
 */
enum class ErrorType {
    /** Generated by a "TODO" note in the ink source */
    Author,
    /** You should probably fix this, but it's not critical */
    Warning,
    /** Critical error that can't be recovered from */
    Error
}

/** Callback for errors throughout both the ink runtime and compiler. */
fun interface ErrorHandler {
    fun error(message: String, type: ErrorType)
}
